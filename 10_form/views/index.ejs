<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        fieldset{
            width: 30%;
        }
    </style>
</head>
<body>
    <h1><%=title %></h1>
    <h4>GET 요청</h4>
    <form action="/getform" method="get">
        <label for="name">이름</label> <!--label의 for와 id를 동일하게 설정 => 이름을 눌러도 Input에 포커싱 가능-->
        <input type="text" placeholder="name" id="name" name="name">
        <fieldset><legend>성별</legend>
            <input type="radio" name="gender" value="남자">남
            <input type="radio" name="gender" value="여자">여
        </fieldset>
        <fieldset><legend>생년월일</legend>
            <select name="year">
                <% for (let i = 1990; i<=2023; i++){ %>
                    <option value="<%=i%>" > <%=i%></option>
               <% } %>
            </select>년
            <select name="month">
               <% for (let i = 1; i<=12; i++){ %>
                    <option value="<%=i%>" > <%=i%></option>
                <% } %>
            </select>월
            <select name="day">
                <% for (let i = 1; i<=31; i++){ %>
                    <option value="<%=i%>" > <%=i%></option>
                <% } %>
            </select>일
        </fieldset>
        <fieldset><legend>관심사</legend>
            <input type="checkbox" name="hobby" value="여행">여행
            <input type="checkbox" name="hobby" value="패션">패션
            <input type="checkbox" name="hobby" value="음식">음식
        </fieldset>
        <button type="submit">회원가입</button>
    </form>
    <hr/>
    <h4>POST 요청</h4>
    <form action="/postform" method="post">
        <form action="/getform" method="get">
            이름<input type="text" placeholder="name" name="name" required>
            <fieldset><legend>성별</legend>
                <input type="radio" name="gender" value="남자" required>남
                <input type="radio" name="gender" value="여자" required>여
            </fieldset>
            <fieldset><legend>생년월일</legend>
                <select name="year">
                    <% for (let i = 1990; i<=2023; i++){ %>
                        <option value="<%=i%>" > <%=i%></option>
                   <% } %>
                </select>년
                <select name="month">
                   <% for (let i = 1; i<=12; i++){ %>
                        <option value="<%=i%>" > <%=i%></option>
                    <% } %>
                </select>월
                <select name="day">
                    <% for (let i = 1; i<=31; i++){ %>
                        <option value="<%=i%>" > <%=i%></option>
                    <% } %>
                </select>일
            </fieldset>
            <fieldset><legend>관심사</legend>
                <input type="checkbox" name="travel" value="여행">여행
                <input type="checkbox" name="fashion" value="패션">패션
                <input type="checkbox" name="food" value="음식">음식
            </fieldset>
        <fieldset><legend>선택</legend>
            좋아하는 색상<input type="color" name="color" value="">
            좋아하는 숫자<input type="number" name="number">
        </fieldset>
        <button type="submit">제출</button>
    </form>
</body>
</html>